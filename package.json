{
  "name": "trmnlocal",
  "devDependencies": {
    "@types/node": "^24.3.0",
    "esbuild": "^0.25.9",
    "fast-png": "^7.0.1",
    "thorish": "^1.1.70"
  },
  "peerDependencies": {
    "puppeteer": "^24.17.0"
  },
  "type": "module",
  "scripts": {
    "build": "esbuild --bundle --format=esm --platform=node --external:puppeteer --outfile=dist/trmnlocal.js src/index.ts",
    "prepublishOnly": "node --run build",
    "start": "node src/index.ts"
  },
  "bin": {
    "trmnlocal": "./dist/bin.js"
  },
  "private": true,
  "license": "Apache-2.0"
}
